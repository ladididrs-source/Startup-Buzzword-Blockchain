[project]
name = 'Startup-Buzzword-Blockchain'
description = ''
authors = []
telemetry = true
cache_dir = '.\.cache'
requirements = []
[contracts.pivot-prediction-algorithm]
path = 'contracts/pivot-prediction-algorithm.clar'
clarity_version = 3
epoch = 'latest'

[contracts.synergy-leverage-optimizer]
path = 'contracts/synergy-leverage-optimizer.clar'
clarity_version = 3
epoch = '3.2'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
